import React from 'react'
import { useDispatch } from 'react-redux'
import { loadData } from '../../redux/actions/Login'

const Home = () => {
  const dispatch = useDispatch()

  return <div>Hello to dada</div>
}

export default Home
